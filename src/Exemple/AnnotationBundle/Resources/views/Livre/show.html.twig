{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Livre</h1> {% include 'ExempleAnnotationBundle:Default:menu.html.twig'%}

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Letitre</th>
                <td>{{ entity.letitre }}</td>
            </tr>
            <tr>
                <th>Leslug</th>
                <td>{{ entity.leslug }}</td>
            </tr>
            <tr>
                <th>Leresume</th>
                <td>{{ entity.leresume }}</td>
            </tr>
            <tr>
                <th>Laparution</th>
                <td>{{ entity.laparution|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('livre') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('livre_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
